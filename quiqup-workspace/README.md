# quiqup-workspace

Private workspace for all Quiqup-related work.

## Structure
- `docs/` — internal documents
- `contacts/` — contact notes (avoid storing personal data if possible)
- `workspace/` — submodule pointing to the public `workspace` repo
